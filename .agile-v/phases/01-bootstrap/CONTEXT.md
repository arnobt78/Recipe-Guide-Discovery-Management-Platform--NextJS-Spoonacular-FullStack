# Phase 01 — Context

## Project

Next.js 15 recipe platform — Spoonacular API, Prisma/PostgreSQL, NextAuth, unified API route.

## Key Paths

- API: `app/api/[...path]/route.ts`
- Client: `src/components/`, `src/hooks/`, `src/context/`
- Server lib: `lib/`
- Docs: `docs/`, `CLAUDE.md`, `SENTRY_ERRORS.md`

## C1 Entry Reason

User requested Agile V Infinity Loop for error fixation, implementation, and extension with traceability.

## Do Not

- Delete existing code without CR
- Skip REQ parent on new artifacts
- Self-verify at Gate 2 (Red Team required)
